pytest>=7.0.0
pytest-asyncio>=0.21.0
httpx>=0.24.0  # Required by TestClient
uvicorn>=0.18.0  # Required for ASGI testing
starlette>=0.20.0  # Required for ASGI testing
coverage>=7.0.0
pretend>=1.0.0  # Required for mocking
